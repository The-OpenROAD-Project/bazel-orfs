module(
    name = "bazel-orfs",
    version = "0.0.1",
    compatibility_level = 1,
)

bazel_dep(name = "patchelf", version = "0.18.0")

orfs = use_extension("//:extension.bzl", "orfs_repositories")
orfs.default(image = "openroad/orfs:f8d87d5bf1b2fa9a7e8724d1586a674180b31ae9")
use_repo(orfs, "com_docker_download")
use_repo(orfs, "docker_orfs")
