--- a/rtl/core/systolic_array.v	2026-03-25 08:03:04.951608535 +0100
+++ b/rtl/core/systolic_array.v	2026-03-25 08:03:19.213695484 +0100
@@ -217,10 +217,7 @@
                 wire [DATA_WIDTH-1:0] weight_in = weight_load_data[row*DATA_WIDTH +: DATA_WIDTH];
                 wire do_clear = clear_acc && (state == S_COMPUTE || (state == S_IDLE && start)) && (cycle_count == 0);
                 
-                mac_pe #(
-                    .DATA_WIDTH(DATA_WIDTH),
-                    .ACC_WIDTH(ACC_WIDTH)
-                ) pe_inst (
+                mac_pe pe_inst (
                     .clk         (clk),
                     .rst_n       (rst_n),
                     .enable      (pe_enable),
