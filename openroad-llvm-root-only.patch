diff --git a/MODULE.bazel b/MODULE.bazel
index ff6b089..e986bf8 100644
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -93,17 +93,9 @@ git_override(
 ## Lock the compiler version and avoid any local compiler
 bazel_dep(name = "toolchains_llvm", version = "1.5.0")
 
-# Configure and register the toolchain.
-llvm = use_extension("@toolchains_llvm//toolchain/extensions:llvm.bzl", "llvm")
-llvm.toolchain(
-    llvm_version = "20.1.8",
-)
-use_repo(llvm, "llvm_toolchain")
-# use_repo(llvm, "llvm_toolchain_llvm") # if you depend on specific tools in scripts
-
-# FYI: Comment out on NixOS where you'd like to use the local clang toolchain.
+# HACK: llvm extension removed - toolchains_llvm enforces root-only usage.
+# The root module must configure the llvm extension instead.
 register_toolchains(
-    "@llvm_toolchain//:all",
     "@rules_verilator//verilator:verilator_toolchain",
 )
 
