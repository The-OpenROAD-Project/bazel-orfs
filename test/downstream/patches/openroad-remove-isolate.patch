Remove isolate = True from npm use_extension to avoid requiring
--experimental_isolated_extension_usages in downstream consumers.

diff --git a/MODULE.bazel b/MODULE.bazel
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -176,7 +176,6 @@ node.toolchain(node_version = "22.14.0")
 npm = use_extension(
     "@aspect_rules_js//npm:extensions.bzl",
     "npm",
-    isolate = True,
 )
 npm.npm_translate_lock(
     name = "npm",
