"""Self (bazel-orfs) fixture for bump tests."""

module(
    name = "bazel-orfs",
    version = "0.0.1",
)

bazel_dep(name = "orfs")
git_override(
    module_name = "orfs",
    commit = "old_orfs_commit",
    remote = "https://github.com/The-OpenROAD-Project/OpenROAD-flow-scripts.git",
)

bazel_dep(name = "openroad")
git_override(
    module_name = "openroad",
    commit = "old_openroad_commit",
    init_submodules = True,
    patch_strip = 1,
    patches = [
        "//patches:openroad-10384-add-openroad-qt.patch",
    ],
    remote = "https://github.com/The-OpenROAD-Project/OpenROAD.git",
)

orfs = use_extension("//:extension.bzl", "orfs_repositories")
orfs.default()
